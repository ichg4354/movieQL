type Query {
  movies: [Movie]
  movie(id: Int): Movie
}

type Mutation {
  remove(id: Int!): Boolean!
  add(title: String!, score: Int!): [Movie]
}

type Movie {
  title: String!
  score: Int!
  id: Int!
}
